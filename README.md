# Minecraft 世界导航地图

基于 Minecraft 1.7.2 创建的世界，种子 `6220807999717207113`，游玩时间约 **280 小时**，在游玩至约 240 小时时升级至 **1.21** 版本。

## 服务器信息

- **核心**：Fabric
- **模式**：生存模式 + 创造模式（主要），仅允许手动放置方块
- **Mods**：`fabric-api`、`dynmap`、`chunky`（均为提升体验的辅助模组）
- **风格**：主打原版体验

## 世界布局

世界分为**生存区**与**创造区**，二者相互独立、互不影响。整体大小约 **3500×4000**，边界限制活动范围约 **5000×5000**。

> ⚠️ 请不要在生存时进入创造区，也不要在创造时进入生存区！

### 出生点

坐标 `(0, 0)`，周围几十格范围的方块设置为**不可破坏**。正下方为一个红石时钟，用于计时服务器运行的真实时间。出生后可选择前往生存区（**不可逆**）或创造区。

### 生存区域

- 主要位于 `x < -700`
- 可能传送至：`(-900, -570)`（靠近创造区，开发程度适中）或 `(-1500, -900)`（开发程度较低）
- 地形：黑森林、平原、森林、沼泽、海洋

### 创造区域

- 主要位于 `x > -700`
- 地形：沙漠、热带草原、河流及少量平原和湖泊
- 此地图**仅包含创造区**的情况

## 城市列表

| 城市 | 坐标 | 规模 |
|------|------|------|
| A 市 | `(-355, 81)` | 最大 |
| B 市 | `(622, -737)` | 大 |
| C 市 | `(-265, -245)` | 较大 |
| D 市 | `(722, -1480)` | 中 |
| E 市 | `(-117, -985)` | 小 |
| F 市 | `(40, -1212)` | 小 |
| G 市 | `(233, -820)` | 小 |
| H 市 | `(350, 122)` | 小 |

城市之间存在**铁路连接**，欢迎探索。出生点靠近 A 市。

以上信息的最后一次更新为2026.7.14

***************以上信息不可由AI更改！****************

本项目主要由AI辅助开发

******************以下信息由AI总结*****************

## 项目结构

```
├── index.html                # 入口页面
├── style.css                 # 样式
├── roads-builder.js          # 道路/铁路/地铁数据构建逻辑
├── app.js                    # 地图渲染、导航、寻路等应用逻辑
├── README.md
├── update_logs/
│   └── what_is_new_in_v*.md  # 版本迭代记录
└── roads/
    ├── citys/
    │   └── city_location.js  # 城市中心坐标
    ├── roads/
    │   ├── A.js ~ H.js       # 各城市道路坐标
    │   └── connection_road.js# 额外连接道路
    ├── railway/
    │   └── railway.js        # 铁路车站、中间点、线路
    └── metro/
        └── metro.js          # 地铁系统数据（线路、车站、出入口、换乘）
```

## 功能概览

### 三种模式
- **👁️ 查看** — 浏览世界地图，缩放平移
- **🧭 导航** — 设置起点/终点，自动寻路（道路 + 铁路）
- **🚇 公共** — 显示地铁网络，寻路包含地铁选项

### 交通网络
- **道路**：各城市内部路网，按等级区分（高速/主干/次干）
- **铁路**：黄色虚线，城市间快速交通，仅在车站上下车
- **地铁**：彩色线路，A/B/C 市区内通勤，可在换乘站换乘，入口登车（惩罚 `15 + 入口↔站台距离/2`）

### 导航系统
- 基于 Dijkstra 算法，支持多模式混合导航
- 铁路-地铁-道路三层图结构，通过换乘边连接
- 各层独立优先级（道路: 1.5~3、铁路: 6、地铁: 6）
- 模式过滤：非公共模式下地铁边不参与寻路
- 路线按段着色：地铁段为线路色，换乘/道路段为红色

### 交互
- 鼠标滚轮缩放、拖拽平移
- 移动端双指缩放、单指平移
- 缩放时自动隐藏城市名称，标记尺寸同步缩小

## 数据说明

- **数据文件**（`roads/` 下各文件）：仅包含坐标等纯数据，修改后刷新即可生效
- **逻辑文件**（`roads-builder.js`、`app.js`）：负责将数据构建为可用的图结构并渲染
- **版本记录**：详见 `update_logs/` 目录

---
以上信息最后一次更新时间：2026.7.23
版本：v0.4


*署名：YXD*
